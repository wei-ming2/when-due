{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Default desktop capability for the main When Due window.",
  "windows": ["main"],
  "permissions": ["core:default", "notification:default"]
}
